# my-project

Full AWS Backend <br/>
Hosting on Render <br/>
Personal API (Server)<br/>
React Application for frontend (Client)<br/>

## To start locally
- Enter the `client` directory.
- Run `npm install` in the CLI.
- Run `npm start`.
